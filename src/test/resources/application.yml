server:
  port: 8000
  servlet:
    context-path: /api
  error:
    path: /api

spring:
  application:
    name: laboratory booking management system
  redis:
    host: @chelinlai.redis.host@
    database: 0
    port: @chelinlai.redis.port@
  profiles:
    include: datasource,app

logging:
  level:
    root: info

